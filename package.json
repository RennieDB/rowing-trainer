{
  "name": "rowing-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based rowing manoeuvring trainer (PoC) — top-down double scull with per-oar physics.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
